# ─────────────────────────────────────────────────────────────────────────────
# Smart Auto Cars — Backend Dependencies
# Database packages (sqlalchemy, psycopg2-binary, alembic) are added
# when we build the database layer in the next phase.
# ─────────────────────────────────────────────────────────────────────────────

# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# Environment variables
python-dotenv==1.0.1

# HTTP client — needed for FastAPI TestClient in tests
httpx==0.27.2

# ── Testing and code quality ──────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.6.9